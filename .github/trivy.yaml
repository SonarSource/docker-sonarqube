# Trivy Configuration File
# Reference: https://aquasecurity.github.io/trivy/latest/docs/references/configuration/config-file/

# Scan settings
scan:
  # Skip scanning of specific directories
  skip-dirs:
    - "node_modules"
    - "vendor"
    - ".git"

# Vulnerability settings
vulnerability:
  # Types of vulnerabilities to detect
  type:
    - os  # Only scan OS packages (excludes application libraries like JAR, npm, etc.)

# Report settings
# NOTE: Trivy's config key for the output format is the top-level `format`;
# a nested `report:` mapping collides with the string `--report` flag and
# makes Trivy fail with an invalid `--report` value.
format: table

# Severity levels to report
severity:
  - CRITICAL
  - HIGH
  - MEDIUM
  - LOW

# Database settings
db:
  # Skip database update (useful for offline environments)
  skip-update: false

# Timeout settings
timeout: 10m
